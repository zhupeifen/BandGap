# Dependencies for scripts/gnn_mp_gap.py (structure-based GNN on mp_gap).
# IMPORTANT: install the CUDA build of torch that matches the cluster's CUDA module,
# e.g. for CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# (or just `module load pytorch` if your cluster provides it, then skip torch here)
torch
torch_geometric
pymatgen
matminer
xgboost
scikit-learn
pandas
numpy
